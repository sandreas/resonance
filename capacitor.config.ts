import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pilabor.resonance',
  appName: 'resonance',
  // webDir: 'dist'
  webDir: 'build'
};

export default config;
